import {
  getProviderApiKeyUrls,
  getProviderKindOptions,
  getProviderPresets
} from "@chengxiaobang/shared";

export const PROVIDER_PRESETS = getProviderPresets();
export const API_KEY_URLS = getProviderApiKeyUrls();
export const PROVIDER_KIND_OPTIONS = getProviderKindOptions();
